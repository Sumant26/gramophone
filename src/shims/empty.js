// Intentionally empty. Used to stub out Node/React-Native-only modules that
// a dependency (jsmediatags) references but that this browser-only app
// never actually executes. See vite.config.js.
export default {}
